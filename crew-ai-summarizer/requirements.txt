# FastAPI e server web
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# CrewAI e dipendenze AI - lasciamo che pip risolva le dipendenze automaticamente
crewai>=0.35.0

# Utilità generali
python-multipart>=0.0.6

# Note per l'installazione:
# 1. Creare virtual environment: python -m venv venv
# 2. Attivare venv: source venv/bin/activate (Linux/Mac) o venv\Scripts\activate (Windows)  
# 3. Installare dipendenze: pip install -r requirements.txt
# 4. Assicurarsi di avere OPENAI_API_KEY nelle variabili d'ambiente

# NOTA: Rimosso pydantic con versione specifica per evitare conflitti
# Pip installerà automaticamente la versione compatibile con tutti i pacchetti